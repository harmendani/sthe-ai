{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "index",
  "scripts": {
    "dev": "yarn predev && tsx src",
    "predev": "run -T tsc --noEmit"
  },
  "dependencies": {
    "@azure/identity": "^4.13.0",
    "fastify": "^5.6.1",
    "openai": "^6.9.1"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
